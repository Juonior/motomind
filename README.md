# Mercedes OBD Telegram Bot

Telegram бот для мониторинга данных OBD-II вашего Mercedes через Bluetooth.

## Возможности

- 📊 Просмотр всех данных OBD (обороты, скорость, температура и т.д.)
- 🌡️ Мониторинг температуры двигателя
- ⚠️ Просмотр и очистка кодов ошибок (DTC)
- 🔌 Управление подключением к OBD адаптеру
- 💬 Удобный интерфейс через Telegram

## Требования

- OBD-II Bluetooth адаптер
- Docker и Docker Compose
- Telegram бот токен

## Установка и запуск

1. Клонируйте репозиторий или скопируйте файлы проекта

2. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

3. Отредактируйте `.env` и укажите:
   - `BOT_TOKEN` - токен вашего Telegram бота (получить у @BotFather)
   - `OBD_PORT` - порт OBD адаптера (например, `/dev/rfcomm0`)
   - `OBD_MAC` - MAC адрес Bluetooth адаптера (опционально, для автоматического создания RFCOMM порта)

4. Подключите OBD адаптер по Bluetooth:

   **Вариант 1 (автоматический):**
   - Укажите `OBD_MAC` в `.env` файле
   - Контейнер автоматически создаст RFCOMM порт при запуске
   
   **Вариант 2 (ручной):**
   ```bash
   # Сканирование устройств
   hcitool scan
   
   # Подключение (замените MAC_ADDRESS на адрес вашего адаптера)
   sudo rfcomm bind 0 MAC_ADDRESS 1
   ```

5. Запустите через Docker Compose:
   ```bash
   docker-compose up -d
   ```

6. Проверьте логи:
   ```bash
   docker-compose logs -f
   ```

## Использование

После запуска бота в Telegram:

- `/start` - главное меню с кнопками
- `/connect` - подключиться к OBD адаптеру
- `/disconnect` - отключиться от OBD адаптера
- `/status` - проверить статус подключения
- `/data` - получить все данные
- `/temperature` - получить температуру
- `/errors` - получить коды ошибок
- `/clear_errors` - очистить коды ошибок

## Настройка Bluetooth в Docker

Для работы с Bluetooth в Docker контейнере используется `privileged: true` и `network_mode: host`. Это необходимо для доступа к Bluetooth устройствам.

Если у вас проблемы с подключением, убедитесь что:
- OBD адаптер подключен и сопряжен с системой
- RFCOMM порт создан (`/dev/rfcomm0`)
- Права доступа настроены правильно

## Структура проекта

```
motomind/
├── bot.py              # Основной файл бота
├── obd_handler.py      # Обработчик OBD подключения
├── config.py           # Конфигурация с pydantic settings
├── requirements.txt    # Зависимости Python
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── .env.example        # Пример файла настроек
└── README.md          # Документация
```

## Примечания

- Бот использует библиотеку `python-OBD` для работы с OBD-II
- Поддерживаются стандартные OBD-II команды
- Некоторые параметры могут быть недоступны в зависимости от модели автомобиля

